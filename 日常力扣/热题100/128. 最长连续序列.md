🏷中等 #并查集 #数组 #哈希表 

给定一个未排序的整数数组 `nums` ，找出数字连续的最长序列（不要求序列元素在原数组中连续）的长度。

请你设计并实现时间复杂度为 `O(n)` 的算法解决此问题。

**示例 1：**
>**输入：`nums = [100,4,200,1,3,2]`
>输出：`4`
>解释：`最长数字连续序列是 [1, 2, 3, 4]。它的长度为 4。`

**示例 2：**
>**输入：`nums = [0,3,7,2,5,8,4,6,0,1]`
>输出：`9`

**示例 3：**
>**输入：`nums = [1,0,1,2]`
>输出：`3`

**提示：**

- `0 <= nums.length <= 105`
- `-109 <= nums[i] <= 109`


# 解题思路与实现

- 一个数 x 只有在满足：x - 1 不在集合中时：x 才是一个连续序列的“起点”。从起点开始向右数：x, x+1, x+2, x+3 ...
- 只需要看起点，非起点的不用看

| **特性维度**  | **哈希集合法 (最优解) 🌟**                                        | **排序法 (非最优解)**                                          |
| --------- | --------------------------------------------------------- | ------------------------------------------------------- |
| **核心思路**  | 使用 **哈希集合** 实现 $O(1)$ 查找，并利用 **"跳过非起点"** 策略，确保每个序列只被检查一次。 | 先对数组进行 **排序** $O(n \log n)$，然后一次遍历排序后的数组来查找连续序列。        |
| **查找机制**  | `if num - 1 not in num_set:` (哈希查找 $O(1)$)                | `if nums[i] == nums[i-1] + 1:` (数组访问 $O(1)$)            |
| **时间复杂度** | **$O(n)$** (线性时间)                                         | $O(n \log n)$ (由排序决定)                                   |
| **空间复杂度** | **$O(n)$** (用于存储哈希集合)                                     | $O(1)$ 或 $O(n)$ (取决于排序算法，Python 通常为 $O(n)$)             |
| **优势**    | **时间复杂度最低**，是渐近最优解，能高效处理大规模数据集。                           | 代码逻辑相对简单，在原地排序时空间复杂度可能较低（虽然 Python 的 `sort` 也是 $O(n)$）。 |
| **劣势**    | 需要额外的 $O(n)$ 空间来存储哈希集合。                                   | 排序操作成为性能瓶颈，运行速度比哈希集合法慢。                                 |

## 方法一：HashSet🌟

```python 
class Solution:
	def longestConsecutive(self, nums: List[int] -> int):
		s = set(nums)
		res = 0
		for num in s:
			if(num - 1) not in s:
				seq = 1
				while num + 1 in s：
					seq += 1
					num += 1
				res = max(seq, res)
		return res 
```

```python
class Solution:
    def longestConsecutive(self, nums: List[int]) -> int:
        num_set = set(nums)
        longest = 0
        for x in num_set:
            if x - 1 not in num_set:  # x 是起点
                length = 1
                while x + length in num_set:
                    length += 1
                longest = max(longest, length)
        
        return longest

```