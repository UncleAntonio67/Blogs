🏷中等 #数组 #哈希表 #前缀和

给你一个整数数组 `nums` 和一个整数 `k` ，请你统计并返回 _该数组中和为 `k` 的子数组的个数_ 。

子数组是数组中元素的连续非空序列。

**示例 1：**

>**输入：`nums = [1,1,1], k = 2`
>输出：`2`

**示例 2：**

>**输入：`nums = [1,2,3], k = 3`
>输出：`2`

**提示：**

- `1 <= nums.length <= 2 * 104`
- `-1000 <= nums[i] <= 1000`
- `-107 <= k <= 107`

# 解题思路与实现

- 暴力解法会超时
- 问题转化为：通过前缀和，任意范围的子数组 【i, j) 的元素和就等于 preSum[j] - preSum[i]。我们要查找的就是前缀和数组中满足 j > i 且 preSum[j] - preSum[i] = k 的下标对 (i,j) 的个数。

![[file-20251029185817771.png]]

|**特性维度**|**分析内容**|
|---|---|
|**核心思路**|**前缀和差值查找**。利用 $\text{Sum}(j, i) = \text{PS}(i) - \text{PS}(j-1) = k$ 的关系，通过哈希表存储历史前缀和 $\text{PS}(j-1)$ 及其次数，从而快速（$O(1)$）查找满足 $\text{PS}(j-1) = \text{PS}(i) - k$ 的匹配项。|
|**时间复杂度**|**$O(n)$**（线性时间）。算法仅进行一次线性遍历，哈希表的查找和更新操作平均时间为 $O(1)$。|
|**空间复杂度**|**$O(n)$**（线性空间）。空间主要用于存储哈希表 (`ps_counts`)，最坏情况下需要存储 $n$ 个不同的前缀和。|
|**效率**|**最优解**。通过牺牲 $O(n)$ 空间，成功将暴力解法的 $O(n^2)$ 时间复杂度优化到了 $O(n)$ 线性时间。|

## 方法一：前缀和法

```python
class Solution:
    def subarraySum(self, nums: List[int], k: int) -> int:
        ps_counts = {0:1}
        res = 0
        ps = 0
        for num in nums:
            ps += num
            res += ps_counts.get(ps-k,0)
            ps_counts[ps] = ps_counts.get(ps,0) + 1
        return res
```