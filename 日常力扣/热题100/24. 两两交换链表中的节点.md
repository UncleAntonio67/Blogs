🏷中等 #递归 #链表 

给你一个链表，两两交换其中相邻的节点，并返回交换后链表的头节点。你必须在不修改节点内部的值的情况下完成本题（即，只能进行节点交换）。

**示例 1：**

![| 300](https://assets.leetcode.com/uploads/2020/10/03/swap_ex1.jpg)

>输入：`head = [1,2,3,4]`
>输出：`[2,1,4,3]`

示例 2：

>输入：`head = []`
>输出：`[]`

示例 3：

>输入：`head = [1]`
>输出：`[1]`

**提示：**

- 链表中节点的数目在范围 `[0, 100]` 内
- `0 <= Node.val <= 100`

# 解题思路与实现

- 迭代+dummy节点
![[file-20251223094332026.png | 400]]

| **维度**    | **内容说明**                                                                                                                                                    |
| --------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **核心思路**  | **双指针（快慢指针）**：利用两个指针之间保持 $n+1$ 的恒定距离。当先行指针（fast）到达末尾（None）时，后行指针（slow）恰好停在待删除节点的前驱位置，从而实现 $O(1)$ 空间下的单次遍历定位。                                                |
| **代码实现**  | `dummy = ListNode(0, head)`<br>`for _ in range(n + 1): fast = fast.next`<br>`while fast: fast, slow = fast.next, slow.next`<br>`slow.next = slow.next.next` |
| **时间复杂度** | $O(L)$ ：其中 $L$ 为链表长度。算法仅对链表进行了一次扫描。                                                                                                                         |
| **空间复杂度** | $O(1)$ ：仅使用了常数个指针变量，不随输入规模增长。                                                                                                                               |

## 方法一：迭代+dummy节点🌟

```python
class Solution:
    def swapPairs(self, head: Optional[ListNode]) -> Optional[ListNode]:
        dummy = ListNode(0,head)
        prev = dummy

        while prev.next and prev.next.next:
            a = prev.next
            b = a.next
            prev.next,a.next,b.next = b,b.next,a
            prev = a
        return dummy.next
```