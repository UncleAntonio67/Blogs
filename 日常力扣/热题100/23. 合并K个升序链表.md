🏷困难 #链表 #分治 #堆 #归并排序 

给你一个链表数组，每个链表都已经按升序排列。

请你将所有链表合并到一个升序链表中，返回合并后的链表。

示例 1：

>输入：`lists = [[1,4,5],[1,3,4],[2,6]]`
>输出：`[1,1,2,3,4,4,5,6]`
>解释：链表数组如下：
>[
>1->4->5,
>1->3->4,
>2->6
>]
>将它们合并到一个有序链表中得到。
>`1->1->2->3->4->4->5->6`

示例 2：

>输入：`lists = []`
>输出：`[]`

示例 3：

>输入：`lists = [[]]`
>输出：`[]`
 

**提示：**

- `k == lists.length`
- `0 <= k <= 10^4`
- `0 <= lists[i].length <= 500`
- `-10^4 <= lists[i][j] <= 10^4`
- `lists[i]` 按 **升序** 排列
- `lists[i].length` 的总和不超过 `10^4`

# 解题思路与实现

- **方法一**：逐一合并
- **方法二**：二分思想，分治合并

|**特性**|**方法一：逐一合并 (Linear)**|**方法二：分治合并 (Divide & Conquer)**|
|---|---|---|
|**合并逻辑**|$(((L1+L2)+L3)+L4)...$|$(L1+L2), (L3+L4) \rightarrow (L12+L34)$|
|**时间复杂度**|$O(K^2 N)$ (性能随 $K$ 增长急剧下降)|**$O(KN \log K)$** (大厂面试标准)|
|**空间复杂度**|$O(1)$|**$O(1)$** (你使用的是迭代法，空间利用率极高)|
|**代码评价**|直观、易写，适合小规模数据。|**最优工程解**，充分利用二分思想减少重复计算。|
## 方法一：逐一合并

```python
class Solution:
    def mergeKLists(self, lists: List[Optional[ListNode]]) -> Optional[ListNode]:
        def merge(l1,l2):
            dummy = p = ListNode(0)
            while l1 and l2:
                if l1.val < l2.val:
                    p.next = l1
                    l1 = l1.next
                else:
                    p.next = l2
                    l2 = l2.next
                p = p.next
            p.next = l1 or l2
            return dummy.next

        if not lists:
            return None
        res = lists[0]
        for i in range(1,len(lists)):
            res = merge(res, lists[i])
        return res
```

## 方法二：分治合并🌟

```python
class Solution:
    def mergeKLists(self, lists: List[Optional[ListNode]]) -> Optional[ListNode]:
        if not lists:
            return None

        def merge(l1, l2):
            dummy = p = ListNode(0)
            while l1 and l2:
                if l1.val < l2.val:
                    p.next, l1 = l1, l1.next
                else:
                    p.next, l2 = l2, l2.next
                p = p.next
            p.next = l1 or l2
            return dummy.next

        interval = 1
        n = len(lists)
        while interval < n:
            for i in range(0, n - interval, interval * 2):
                lists[i] = merge(lists[i], lists[i + interval])
            interval *= 2

        return lists[0]
```