🏷简单 #树 #深度优先搜索 #广度优先搜索 #二叉树 

给你一棵二叉树的根节点 `root` ，翻转这棵二叉树，并返回其根节点。

**示例 1：**

![|400](https://assets.leetcode.com/uploads/2021/03/14/invert1-tree.jpg)

>输入：`root = [4,2,7,1,3,6,9]`
>输出：`[4,7,2,9,6,3,1]`

**示例 2：**

![|400](https://assets.leetcode.com/uploads/2021/03/14/invert2-tree.jpg)

>输入：`root = [2,1,3]`
>输出：`[2,3,1]`

示例 3：

>输入：`root = []`
>输出：`[]`

**提示：**

- 树中节点数目范围在 `[0, 100]` 内
- `-100 <= Node.val <= 100`

# 解题思路与实现

- 递归法

| **维度**    | **方法一：递归 (DFS)**                    | **方法二：迭代 (BFS)**                      |
| --------- | ----------------------------------- | ------------------------------------- |
| **核心机制**  | 利用**系统调用栈**，自顶向下触发交换，再递归处理子树。       | 利用**显式队列 (Queue)**，按层访问节点并立即交换其左右孩子。  |
| **代码逻辑**  | `交换 -> 递归左 -> 递归右` (前序遍历风格)         | `入队 -> 出队并交换 -> 子节点入队` (层序遍历风格)       |
| **空间消耗**  | **系统栈空间**。                          | **堆内存空间**（用于存储队列节点）。                  |
| **时间复杂度** | **$O(n)$**：每个节点必须且仅访问一次。            | **$O(n)$**：每个节点必须且仅入队/出队一次。           |
| **空间复杂度** | **$O(h)$**：取决于树的高度 $h$。最坏情况 $O(n)$。 | **$O(w)$**：取决于树的最大宽度 $w$。最坏情况 $O(n)$。 |

## 方法一：递归

```python
class Solution:
    def invertTree(self, root: Optional[TreeNode]) -> Optional[TreeNode]:
        if not root:
            return
        root.left, root.right = root.right,root.left
        self.invertTree(root.left)
        self.invertTree(root.right)
        return root
```