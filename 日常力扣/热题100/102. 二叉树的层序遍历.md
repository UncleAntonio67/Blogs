🏷中等 #树 #二叉树 #广度优先搜索 

给你二叉树的根节点 `root` ，返回其节点值的 **层序遍历** 。 （即逐层地，从左到右访问所有节点）。

**示例 1：**

![| 200](https://assets.leetcode.com/uploads/2021/02/19/tree1.jpg)

>输入：`root = [3,9,20,null,null,15,7]`
>输出：`[[3],[9,20],[15,7]]`

示例 2：

>输入：`root = [1]`
>输出：`[[1]]`

示例 3：

>输入：`root = []`
>输出：`[]`

**提示：**

- 树中节点数目在范围 `[0, 2000]` 内
- `-1000 <= Node.val <= 1000`


# 解题思路与实现

- 递归

| **维度**         | **方法一：DFS**                | **方法二：BFS (队列迭代)**          |
| -------------- | -------------------------- | --------------------------- |
| **访问顺序**       | **纵向**。先深入一条路径，再回溯。        | **横向**。一层一层完整访问。            |
| **空间复杂度 (平均)** | **$O(h)$**。取决于树的高度（递归栈）。   | **$O(w)$**。取决于树的最大宽度（队列容量）。 |
| **时间复杂度**      | **$O(n)$**。遍历每个节点一次。       | **$O(n)$**。遍历每个节点一次。        |
| **数据结构**       | 系统隐式调用栈 + 结果列表索引。          | 显式队列 (`collections.deque`)。 |
| **适用性**        | 在 Python 中代码简洁，适合树高度较小的情况。 | 最正宗的层序遍历，适合处理流式数据或超大树。      |
## 方法一：递归

```python
class Solution:
    def levelOrder(self, root: Optional[TreeNode]) -> List[List[int]]:
        res = []

        def dfs(node,level):
            if not node:
                return
            if len(res) == level:
                res.append([])
            res[level].append(node.val)
            dfs(node.left, level+1)
            dfs(node.right, level+1)
        dfs(root,0)
        return res
```

## 方法二：BFS

```python
from collections import deque

class Solution:
    def levelOrder(self, root: Optional[TreeNode]) -> List[List[int]]:
        if not root: return []
        res, queue = [], deque([root])
        
        while queue:
            level_nodes = []
            # 这一步是关键：固定当前层的数量
            for _ in range(len(queue)):
                node = queue.popleft()
                level_nodes.append(node.val)
                if node.left: queue.append(node.left)
                if node.right: queue.append(node.right)
            res.append(level_nodes)
        return res
```