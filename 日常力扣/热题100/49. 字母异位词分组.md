🏷简单 #数组 #哈希表 #字符串 #排序  

给你一个字符串数组，请你将 字母异位词 组合在一起。可以按任意顺序返回结果列表。

**示例 1:**

>**输入:** strs = `["eat", "tea", "tan", "ate", "nat", "bat"]`
>**输出:**   `[["bat"],["nat","tan"],["ate","eat","tea"]]

**解释：**
- 在 strs 中没有字符串可以通过重新排列来形成 `"bat"`。
- 字符串 `"nat"` 和 `"tan"` 是字母异位词，因为它们可以重新排列以形成彼此。
- 字符串 `"ate"` ，`"eat"` 和 `"tea"` 是字母异位词，因为它们可以重新排列以形成彼此。

**示例 2:**

>**输入:** `strs = [""]`
>**输出:**  `[[" "]]

**示例 3:**

>**输入:** strs = `["a"]`
>**输出:** `[["a"]]

**提示：**

- `1 <= strs.length <= 104`
- `0 <= strs[i].length <= 100`
- `strs[i]` 仅包含小写字母

# 解题思路与实现

- **方法一**：异位词分组，每一组异位词排序后一定是一样的，所以利用排序后的词作为键，将分组作为值。
- **方法二**：可以使用 **字符计数 (Character Counting)** 来生成键，这种方法可以进一步将键生成的时间复杂度从 $O(K \log K)$ 优化到 **$O(K)$**。**思路：** 使用一个包含 26 个整数的元组（tuple）来表示每个字母的出现次数。例如，`"aab"` $\rightarrow$ `(2, 1, 0, ..., 0)`。元组是可哈希的，可以用作字典的键。

| **特性**     | **方法一：字符排序法**               | **方法二：字符计数法 🌟**       |
| ---------- | --------------------------- | ---------------------- |
| **核心思想**   | 将字符串排序作为键 (Canonical Form)。 | 使用字符出现次数的元组作为键。        |
| **键的生成**   | `''.join(sorted(s))`        | `tuple(count)`         |
| **键生成时间**  | $O(K \log K)$               | $O(K)$                 |
| **总时间复杂度** | **$O(N \cdot K \log K)$**   | **$O(N \cdot K)$**     |
| **空间复杂度**  | $O(N \cdot K)$（存储结果和键）      | $O(N \cdot K)$（存储结果）   |
| **优势**     | 代码简洁，易于理解。                  | **时间复杂度更优，理论上更快。**     |
| **劣势**     | 排序操作引入 $O(\log K)$ 因子。      | 键的表示较长，需要额外的计算来生成计数数组。 |

## 方法一：字典法

```python
class Solution:
    def groupAnagrams(self, strs: List[str]) -> List[List[str]]:
    d = defaultdict(list)
    for s in strs:
	    d[''.join(sorted(s))].append(s)
	   return list(d.values())
```

## 方法二：字典法🌟

```python
class SolutionAlternative:
    def groupAnagrams(self, strs: list[str]) -> list[list[str]]:
        res_map = defaultdict(list)
        for s in strs:
            # 创建一个长度为 26 的列表/元组，用于存储 a-z 的计数
            count = [0] * 26 
            for char in s:
                # 记录字符出现次数 (通过 ASCII 码计算偏移量)
                # ord(char) - ord('a') 得到 'a' -> 0, 'b' -> 1, ...
                count[ord(char) - ord('a')] += 1
            
            # 使用元组作为键 (元组是不可变且可哈希的)
            key = tuple(count)
            res_map[key].append(s)
            
        return list(res_map.values())
```
