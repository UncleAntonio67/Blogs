🏷简单 #数组 #双指针 

给定一个数组 `nums`，编写一个函数将所有 `0` 移动到数组的末尾，同时保持非零元素的相对顺序。

**请注意** ，必须在不复制数组的情况下原地对数组进行操作。

**示例 1:**

>**输入:** `nums = [0,1,0,3,12]`
>**输出:** `[1,3,12,0,0]`

**示例 2:**

>**输入:** `nums = [0]`
>**输出:** `[0]`

**提示**:

- `1 <= nums.length <= 104`
- `-231 <= nums[i] <= 231 - 1`

# 解题思路与实现

- 利用数组可以增加删除元素的特性，去将等于0的元素添加到数组后，并置为特殊字符，然后删除
- 利用双指针法，快指针遍历，慢指针保证它右边的元素均为不为0，遍历完成后把慢指针右边的元素全部置为0

| **特性维度**  | **标记/列表操作法**                                                | **双指针原地修改法 🌟**               |
| --------- | ----------------------------------------------------------- | ----------------------------- |
| **核心思路**  | 遍历时，将零 `append` 到末尾，并在原位置做标记 (`"?"`)，最后重新构建列表以移除标记。         | 使用 **快慢指针**，只将非零元素前移，最后填充零。   |
| **关键操作**  | `nums.append()`, `nums[i] = "?"`, 列表推导式 (`nums[:] = [...]`) | 索引赋值 (`nums[slow] = nums[i]`) |
| **时间复杂度** | **$O(n^2)$**                                                | **$O(n)$** (线性时间)             |
| **空间复杂度** | **$O(n)$**                                                  | **$O(1)$** (原地修改)             |
| **优点**    | 逻辑相对直观（像把零“移”到后面）。                                          | **效率最高**，满足原地修改要求，操作次数最少。     |
| **缺点**    | 频繁的 `append` 和列表赋值/重构操作导致效率极低，且不满足原地修改要求。                   | 概念上略复杂，需要理解快慢指针的移动机制。         |

## 方法一：集合遍历

```python
class Solution:
    def moveZeroes(self, nums: List[int]) -> None:
        for i in range(len(nums)):
            if nums[i] == 0:
                nums.append(nums[i])
                nums[i] = "?"
        nums[:] = [x for x in nums if x != "?"]
```

## 方法二：双指针🌟

```python
class Solution:
    def moveZeroes(self, nums: List[int]) -> None:
        slow = 0
        for fast in range(len(nums)):
            if nums[fast] != 0:
                nums[slow] = nums[fast]
                slow += 1
        for i in range(slow, len(nums)):
            nums[i] = 0
```

```python
class Solution:
    def moveZeroes(self, nums: List[int]) -> None:
        """
        Do not return anything, modify nums in-place instead.
        """
        slow = 0
        for i in range(len(nums)):
            if nums[i] != 0:
                nums[slow] = nums[i]
                slow += 1
        for i in range(slow,len(nums)):
            nums[i] = 0
```