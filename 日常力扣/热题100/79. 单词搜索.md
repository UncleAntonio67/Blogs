🏷中等 #深度优先搜索 #数组 #字符串 #回溯 #矩阵 

给定一个 `m x n` 二维字符网格 `board` 和一个字符串单词 `word` 。如果 `word` 存在于网格中，返回 `true` ；否则，返回 `false` 。

单词必须按照字母顺序，通过相邻的单元格内的字母构成，其中“相邻”单元格是那些水平相邻或垂直相邻的单元格。同一个单元格内的字母不允许被重复使用。

**示例 1：**

![ | 200](https://assets.leetcode.com/uploads/2020/11/04/word2.jpg)

>输入：`board = [['A','B','C','E'],['S','F','C','S'],['A','D','E','E']], word = "ABCCED"`
>输出：`true`

**示例 2：**

![ |200](https://assets.leetcode.com/uploads/2020/11/04/word-1.jpg)

>输入：`board = [['A','B','C','E'],['S','F','C','S'],['A','D','E','E']], word = "SEE"`
>输出：`true`

**示例 3：**

![200](https://assets.leetcode.com/uploads/2020/10/15/word3.jpg)

>输入：`board = [['A','B','C','E'],['S','F','C','S'],['A','D','E','E']], word = "ABCB"`
>输出：`false`

**提示：**

- `m == board.length`
- `n = board[i].length`
- `1 <= m, n <= 6`
- `1 <= word.length <= 15`
- `board` 和 `word` 仅由大小写英文字母组成

**进阶：你可以使用搜索剪枝的技术来优化解决方案，使其在 `board` 更大的情况下可以更快解决问题？

# 解题思路与实现

最关键的只有 3 点：
1. 每个起点 i, j 去尝试 dfs
因为 `word[0]` 可以从任何一个格子开始。
2. dfs 的含义是：从 `board[i][j] `开始能不能匹配 `word[k]`
并尝试走四个方向。
3. 必须有 visited（这里用 `board[i][j]` = '#' 临时占位）
否则你会走回头路。

| **维度**    | **分析内容**                                                                                                                                                                                                        |
| --------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **核心思路**  | **深度优先搜索 (DFS) + 回溯**。<br>1. **起点遍历**：外层双循环遍历矩阵，寻找与 `word[0]` 匹配的格点作为 DFS 起点。<br>2. **标记已访问**：将当前格点改为 `'#'`，防止在同一次路径搜索中“回头”重复使用。<br>3. **四连通探索**：向上下左右四个方向递归。<br>4. **状态恢复**：DFS 结束后将 `'#'` 改回原字母，确保不影响其他起点的搜索。 |
| **空间复杂度** | $O(L)$<br>其中 $L$ 为单词 `word` 的长度。这是递归调用栈的最大深度。                                                                                                                                                                   |
| **时间复杂度** | $O(M \times N \times 3^L)$<br>$M, N$ 为网格尺寸。每次搜索有 3 个新方向（除去来的方向），总深度为 $L$。                                                                                                                                       |
| **优点**    | 1. **节省空间**：使用 `board[i][j] = '#'` 代替了额外的 `visited` 矩阵，空间复杂度极低。<br>2. **剪枝高效**：一旦字符不匹配或出界立即返回 `False`。                                                                                                          |
## 方法一：

```python
class Solution:
    def exist(self, board: List[List[str]], word: str) -> bool:
        m, n = len(board), len(board[0])

        def dfs(i,j,k):
            if k == len(word):
                return True
            if i < 0 or i >= m or j < 0 or j>=n:
                return False
            if board[i][j] != word[k]:
                return False
            tmp = board[i][j]
            board[i][j] = '#'

            found = (dfs(i+1, j, k+1) or
            dfs(i-1, j, k+1) or
            dfs(i, j+1, k+1) or
            dfs(i, j-1, k+1))

            board[i][j] = tmp
            return found
        for i in range(m):
            for j in range(n):
                if dfs(i,j,0):
                    return True
        return False
```