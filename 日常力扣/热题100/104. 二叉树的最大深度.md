🏷简单 #树 #深度优先搜索 #二叉树 

给定一个二叉树 `root` ，返回其最大深度。

二叉树的 **最大深度** 是指从根节点到最远叶子节点的最长路径上的节点数。

**示例 1：**

![|300 ](https://assets.leetcode.com/uploads/2020/11/26/tmp-tree.jpg)

>输入：`root = [3,9,20,null,null,15,7]`
>输出：`3`

示例 2：

>输入：`root = [1,null,2]`
>输出：`2`

**提示：**

- 树中节点的数量在 `[0, 104]` 区间内。
- `-100 <= Node.val <= 100`

# 解题思路与实现

- 递归
- 迭代

|**维度**|**方法一：递归 (DFS)**|**方法二：迭代 (BFS)**|
|---|---|---|
|**搜索策略**|**自底向上**。先钻到叶子节点，再回溯累加高度。|**自顶向下**。按层访问，每处理完一层，深度加 1。|
|**核心数据结构**|**系统调用栈**。利用递归函数的回溯机制。|**队列 (`collections.deque`)**。用于存储当前层及下一层的节点。|
|**空间复杂度**|**$O(h)$**。取决于树的高度。最坏情况下（链状树）为 $O(n)$。|**$O(w)$**。取决于树的最大宽度（通常出现在完全二叉树的底层）。|
|**直观理解**|像“潜水”，先到底再计算浮上来的距离。|像“扫描”，一层一层往下扫，数扫了多少次。|
|**代码逻辑**|解决子问题：`max(左深, 右深) + 1`。|逐层处理：清空当前队列内所有节点后 `depth++`。|

## 方法一：递归

```python
class Solution:
    def maxDepth(self, root: Optional[TreeNode]) -> int:
        if not root:
            return 0
        left_depth = self.maxDepth(root.left)
        right_depth = self.maxDepth(root.right)
        return max(left_depth, right_depth) + 1
```

## 方法二：迭代

```python 
class Solution:
    def maxDepth(self, root: Optional[TreeNode]) -> int:
        if not root:
            return 0
        queue = deque([root])
        depth = 0
        while queue:
            level_size = len(queue)
            for _ in range(level_size):
                node = queue.popleft()
                if node.left:
                    queue.append(node.left)
                if node.right:
                    queue.append(node.right)
            depth += 1
        return depth
```